name: build

on:
  push:
    paths:
      - '.github/workflows/build.yml'
      - 'yarn.lock'
      - '**/*.ts'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn
      - run: yarn build
