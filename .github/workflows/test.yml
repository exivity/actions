name: test

on: push

jobs:
  test-init-ssh:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v1
      - uses: ./init-ssh
        with:
          private-key: ${{ secrets.PRIVATE_KEY }}

  test-rabbitmq:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v1
      - uses: ./rabbitmq
        with:
          private-key: ${{ secrets.PRIVATE_KEY }}
