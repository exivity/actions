name: accept-test

on:
  push:
    paths:
      - '.github/workflows/accept-test.yml'
      - 'accept/**'
      - 'lib/github.ts'

jobs:
  accept-test:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - run: echo 'Hello world!'
