name: accept test

on:
  push:
    paths:
      - '.github/workflows/accept-test.yml'
      - 'accept/**'
      - 'lib/github.ts'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Hello world!'
